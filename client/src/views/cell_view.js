const PubSub = require('../helpers/pub_sub.js');
const Request = require('../helpers.request.js');

const CellView = function (container) {
  this.container = container;

}

model.exports = CellView;
